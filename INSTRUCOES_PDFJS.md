# PDF.js setup

See **[README.md](README.md)** — **Install** section.

Quick command:

```bash
npm run copy-pdfjs
```

This copies `pdf.min.js` and `pdf.worker.min.js` into `lib/`. Reload the extension in `chrome://extensions` after running it.
